language: node_js

node_js:
    - "node"
    - "11"
    - "10"

addons:
  apt:
    packages:
      - google-chrome-stable

git:
    submodules: false

before_script:
    - npm install

script:
    - npm test
