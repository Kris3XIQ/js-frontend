language: node_js

node_js:
    - "node"
    - "11"
    - "10"

sudo: false

git:
    submodules: false
  
addons:
  apt:
    update: true
    sources:
      - google chrome
    packages:
      - google-chrome-stable

before_script:
    - npm install

script:
    - npm test
